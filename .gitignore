# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Leaderboard database (sqlite + wal/shm sidecars)
data

# Server-local deploy settings. .env.example is the tracked template; the
# real file belongs to the host it runs on and must survive a deploy pull.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
