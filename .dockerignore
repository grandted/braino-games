# Everything the image builds for itself, plus everything it must never carry.
node_modules
dist
dist-ssr

# The leaderboard database. Never bake real scores into an image — it is
# mounted at runtime instead.
data

.git
.gitignore
.claude
.vscode
.idea
docs
*.log
*.local
.DS_Store
Dockerfile
.dockerignore
docker-compose.yml

# Server-local deploy settings. Compose reads these on the host; baking a
# particular host's bind address into the image would be a trap.
.env
.env.example
