{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* Node runs these files directly by stripping types — no DOM, no vite. */
    "lib": ["ES2023"],
    "types": ["node"],
    "module": "nodenext",
    "moduleResolution": "nodenext"
  },
  "include": ["server", "vite.config.ts"]
}
